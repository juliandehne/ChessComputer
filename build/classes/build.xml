
<project name="chesscomputer" basedir="." default="scala.run">
    <property
    name="sources.dir"
    value="${basedir}"
     />
    <property
    name="build.dir"
    value="${basedir}/../ownbuild"
     />
    <property
    name="scala.home"
    value='C:\Users\Julian\Software\scala-2.9.1.final\scala-2.9.1.final'
     />
    
    <target name="java.compile">        
        <javac srcdir="${sources.dir}"
         destdir="${build.dir}"
         extdirs="${build.dir}/chesscomputer/scala"
         includes="${sources.dir}/**"
    />    
    </target>
     
    <target name="scala.init" depends="java.compile">
        <property
      name="scala-library.jar"
      value="${scala.home}/lib/scala-library.jar"
       />
        <path id="build.classpath">
            <pathelement location="${scala-library.jar}"   />
      <!--<pathelement location="${your.path}"   />-->
            <pathelement location="${build.dir}"   />
        </path>
        <taskdef resource="scala/tools/ant/antlib.xml">
            <classpath>
                <pathelement location="${scala.home}/lib/scala-compiler.jar"   />
                <pathelement location="${scala-library.jar}"   />
            </classpath>
        </taskdef>
    </target>
    
    <target name="scala.build" depends="scala.init">
        <mkdir dir="${build.dir}"   />
        <scalac srcdir="${sources.dir}"
            destdir="${build.dir}"
            classpathref="build.classpath">      
        </scalac>
    </target>
    
    <target name="scala.run" depends="scala.build">
        <exec dir="${basedir}" executable="C:\Users\Julian\Software\scala-2.9.1.final\scala-2.9.1.final\bin\scala.bat">
            <arg line="-cp ${build.dir} chesscomputer.scala.Main" />
        </exec>
    </target>
</project>